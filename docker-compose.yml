version: '3'

services:
  pactman-dev:
    build:
      context: lib
      args:
        - MY_VAR=1
    container_name: pactman-dev
    stdin_open: true
    tty: true
    volumes:
      - ./lib:/code
      - ./lib/.dev-container-history:/root/.history
